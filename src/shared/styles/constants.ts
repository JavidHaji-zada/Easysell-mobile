import { responsiveFontSize } from "react-native-responsive-dimensions";

export default {
    "FONT_SIZE": {
        "12": responsiveFontSize(1.5),
        "15": responsiveFontSize(1.9),
        "16": responsiveFontSize(2)
    },
}